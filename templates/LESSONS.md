# Lessons

<!-- Standing conclusions that outlive a single hypothesis. A lesson earns a
place here when it would change how a future attempt is built, on this
hypothesis or another. Everything else stays in the dossier or the attempt's
NOTES.md.

Lessons are never deleted. A lesson that turns out wrong is marked
`superseded by <id>` and stays; a lesson two holders disagree about is marked
`contested: <pointer>` and stays. The history of what the campaign believed
is part of the evidence.

Entry shape:

## <holder>-L<n>: short title
Claim: one sentence, falsifiable.
Evidence: attempt ids and paths to the artifacts that support it.
Scope: which hypotheses, which assets, or campaign-independent.
Status: active | contested: <pointer> | superseded by <id>
-->

(no lessons yet)
